"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getFuncAndArguments = exports.getProperty = void 0;
function getProperty(object) {
    if (!object)
        return [];
    const prototypeProps = Object.getOwnPropertyNames(Object.getPrototypeOf(object));
    const ownProps = Object.getOwnPropertyNames(object);
    const allProps = [...prototypeProps, ...ownProps].filter((name) => !name.startsWith("_")).sort();
    return allProps;
}
exports.getProperty = getProperty;
function getFuncAndArguments(line) {
    const funcName = line.split("(")[0];
    const argumentsRegex = /(?<=\()(.+?)(?=\))/g;
    const separator = /\,\s*/g;
    const parameters = (line.match(argumentsRegex) || [""])[0].trim().split(separator);
    return { funcName, parameters };
}
exports.getFuncAndArguments = getFuncAndArguments;
//# sourceMappingURL=utils.js.map